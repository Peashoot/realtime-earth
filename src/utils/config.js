// 配置文件
export const CONFIG = {
  // IP定位配置
  location: {
    enableIPLocation: true, // 是否启用IP定位
  },

  // 和风天气API配置
  weather: {
    apiKey: import.meta.env.VITE_QWEATHER_API_KEY || '', // 从环境变量读取
    baseUrl: import.meta.env.VITE_QWEATHER_BASE_URL || 'https://devapi.qweather.com/v7', // 免费版API地址
    updateInterval: 30 * 60 * 1000, // 30分钟更新一次

    // 默认位置（当定位失败时使用）
    defaultLocation: {
      name: '北京',
      lat: 39.9042, 
      lon: 116.4074
    }
  },

  // 地球渲染配置
  earth: {
    textureQuality: '2k', // '2k' | '8k'

    // 太阳赤纬角（模拟季节，单位：度）
    // 'auto': 根据当前日期自动计算（推荐）
    // -23.44: 冬至（南极圈内极昼，北极圈内极夜）
    //    0.0: 春秋分（南北极各一半昼夜）
    // +23.44: 夏至（北极圈内极昼，南极圈内极夜）
    sunDeclination: 'auto',

    // 经度偏移量（度）
    // 用于修正地球贴图旋转导致的经度偏移
    // 如果东经180度显示在本初子午线位置，设置为 180
    longitudeOffset: 90,

    // 是否显示经线和标签
    showMeridianLines: false,

    // 相机控制配置
    camera: {
      enableRotate: false,      // 是否允许鼠标拖动旋转
      enableZoom: false,         // 是否允许滚轮缩放
      distance: 3.0,            // 相机距离（1.5 - 5.0）
      autoRotate: true,        // 是否自动旋转观察视角
      autoRotateSpeed: 0.1      // 自动旋转速度（-10.0 到 10.0，负值为反向）
    },

    // 天体系统配置
    celestial: {
      enabled: true,            // 是否启用天体系统
      showOrbits: false,        // 是否显示轨道线
      timeScale: 1000,          // 时间缩放倍数（1=真实时间，1000=加速1000倍）

      // 太阳视觉效果
      sun: {
        enablePulsate: true,    // 是否启用太阳脉动效果
        enableCorona: true      // 是否启用日冕效果
      },

      // 各个天体的显示开关
      bodies: {
        moon: true,             // 月球
        sun: true,              // 太阳
        mercury: true,          // 水星
        venus: true,            // 金星
        mars: true,             // 火星
        jupiter: true,          // 木星
        saturn: true,           // 土星
        uranus: true,           // 天王星
        neptune: true,          // 海王星
        comet: false             // 彗星
      }
    },

    // 性能优化配置
    performance: {
      // 阴影配置
      enableShadows: false,           // 是否启用阴影（性能敏感，建议中高端设备开启）
      shadowMapSize: 1024,            // 阴影贴图尺寸（512/1024/2048，越大越清晰但越耗性能）

      // 后期处理配置
      enablePostProcessing: false,     // 是否启用后期处理（Bloom等效果）
      bloomStrength: 0.5,             // Bloom 强度（0.0-2.0）
      bloomRadius: 0.4,               // Bloom 半径（0.0-1.0）
      bloomThreshold: 0.85,           // Bloom 阈值（0.0-1.0，越高越少发光）

      // 性能监控
      enableMonitoring: false,         // 是否启用 FPS 监控（开发模式推荐）
    }
  }
}

// 天气图标映射（和风天气代码 → emoji）
export const WEATHER_ICONS = {
  // 晴
  '100': '☀️',  // 晴（白天）
  '150': '🌙',  // 晴（夜间）

  // 多云
  '101': '🌤️',  // 多云（白天）
  '102': '☁️',  // 少云（白天）
  '103': '⛅',  // 晴间多云（白天）
  '151': '☁️',  // 多云（夜间）
  '152': '☁️',  // 少云（夜间）
  '153': '⛅',  // 晴间多云（夜间）

  // 阴
  '104': '☁️',  // 阴

  // 雨
  '300': '🌦️',  // 阵雨（白天）
  '301': '🌧️',  // 强阵雨（白天）
  '302': '⛈️',  // 雷阵雨
  '303': '⛈️',  // 强雷阵雨
  '304': '⛈️',  // 雷阵雨伴有冰雹
  '305': '🌧️',  // 小雨
  '306': '🌧️',  // 中雨
  '307': '🌧️',  // 大雨
  '308': '🌧️',  // 极端降雨
  '309': '🌧️',  // 毛毛雨/细雨
  '310': '🌧️',  // 暴雨
  '311': '🌧️',  // 大暴雨
  '312': '🌧️',  // 特大暴雨
  '313': '🌧️',  // 冻雨
  '314': '🌧️',  // 小到中雨
  '315': '🌧️',  // 中到大雨
  '316': '🌧️',  // 大到暴雨
  '317': '🌧️',  // 暴雨到大暴雨
  '318': '🌧️',  // 大暴雨到特大暴雨
  '350': '🌧️',  // 阵雨（夜间）
  '351': '🌧️',  // 强阵雨（夜间）

  // 雪
  '400': '🌨️',  // 小雪
  '401': '❄️',  // 中雪
  '402': '❄️',  // 大雪
  '403': '❄️',  // 暴雪
  '404': '🌨️',  // 雨夹雪
  '405': '🌨️',  // 雨雪天气
  '406': '🌨️',  // 阵雨夹雪（白天）
  '407': '❄️',  // 阵雪（白天）
  '408': '❄️',  // 小到中雪
  '409': '❄️',  // 中到大雪
  '410': '❄️',  // 大到暴雪
  '456': '🌨️',  // 阵雨夹雪（夜间）
  '457': '❄️',  // 阵雪（夜间）

  // 雾霾等
  '500': '🌫️',  // 薄雾
  '501': '🌫️',  // 雾
  '502': '🌫️',  // 霾
  '503': '🌫️',  // 扬沙
  '504': '🌫️',  // 浮尘
  '507': '💨',  // 沙尘暴
  '508': '💨',  // 强沙尘暴
  '509': '🌫️',  // 浓雾
  '510': '🌫️',  // 强浓雾
  '511': '🌫️',  // 中度霾
  '512': '🌫️',  // 重度霾
  '513': '🌫️',  // 严重霾
  '514': '🌫️',  // 大雾
  '515': '🌫️',  // 特强浓雾

  // 其他
  '900': '🌡️',  // 热
  '901': '🥶',  // 冷
  '999': '❓',  // 未知
}

// 获取天气图标
export function getWeatherIcon(code) {
  return WEATHER_ICONS[code] || '⛅'
}
